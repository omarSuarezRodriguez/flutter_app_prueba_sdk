import 'package:flutter/material.dart';

class App_10 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('App 10', style: TextStyle(fontSize: 40.0)),
    );
  }
}