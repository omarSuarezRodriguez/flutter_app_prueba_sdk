import 'package:flutter/material.dart';

class Export extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('EXPORTAR', style: TextStyle(fontSize: 40.0)),
    );
  }
}