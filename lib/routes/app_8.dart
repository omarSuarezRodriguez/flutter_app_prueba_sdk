import 'package:flutter/material.dart';

class App_8 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('App 8', style: TextStyle(fontSize: 40.0)),
    );
  }
}